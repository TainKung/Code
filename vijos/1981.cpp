#include <cstdio>
#include <cstring>

const int kMax=50010;
int stone[kMax];

bool check(int dis);

int main() {
    int l, n, m;
    scanf("%d%d%d", &l, &n, &m);
    for (int i=1; i<=n; i++) scanf("%d", stone+i);

    int i=1, j=l, ans;
    while (i != j) {
        ans
    } 
}
